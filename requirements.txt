# CloudHosting AI Panel — pinned runtime deps.
# Re-verify versions before baking the golden image.
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
httpx==0.28.1
PyYAML==6.0.2
